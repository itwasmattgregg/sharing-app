# Migration `20200808134848-migration`

This migration has been generated by Matt Gregg at 8/8/2020, 1:48:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Item" (
"borrowerId" INTEGER   ,"description" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"ownerId" INTEGER NOT NULL  ,"title" TEXT NOT NULL  ,"visible" BOOLEAN NOT NULL  ,FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("borrowerId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Item" ("description", "id", "ownerId", "title", "visible") SELECT "description", "id", "ownerId", "title", "visible" FROM "quaint"."Item"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Item";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Item" RENAME TO "Item";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200808134327-migration..20200808134848-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -25,19 +25,22 @@
   createdAt DateTime @default(now())
 }
 model User {
-  id         Int      @id @default(autoincrement())
-  name       String
-  email      String   @unique
-  createdAt  DateTime @default(now())
-  itemsOwned Item[]   @relation(name: "owned")
+  id            Int      @id @default(autoincrement())
+  name          String
+  email         String   @unique
+  createdAt     DateTime @default(now())
+  itemsOwned    Item[]   @relation("ItemsOwned")
+  itemsBorrowed Item[]   @relation("ItemsBorrowed")
 }
 model Item {
   id          Int     @id @default(autoincrement())
   title       String
   description String
   visible     Boolean
-  owner       User    @relation(fields: [ownerId], references: [id])
+  owner       User    @relation("ItemsOwned", fields: [ownerId], references: [id])
   ownerId     Int
+  borrower    User?   @relation("ItemsBorrowed", fields: [borrowerId], references: [id])
+  borrowerId  Int?
 }
```


