# Migration `20200813101547-migration`

This migration has been generated by Matt Gregg at 8/13/2020, 10:15:47 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Item" (
"borrowerId" INTEGER   ,"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"description" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"ownerId" INTEGER NOT NULL  ,"title" TEXT NOT NULL  ,"visible" BOOLEAN NOT NULL  ,FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("borrowerId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Item" ("borrowerId", "description", "id", "ownerId", "title", "visible") SELECT "borrowerId", "description", "id", "ownerId", "title", "visible" FROM "quaint"."Item"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Item";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Item" RENAME TO "Item";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200813100805-migration..20200813101547-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -25,13 +25,14 @@
   itemsBorrowed Item[]   @relation("ItemsBorrowed")
 }
 model Item {
-  id          Int     @id @default(autoincrement())
+  id          Int      @id @default(autoincrement())
+  createdAt   DateTime @default(now())
   title       String
   description String
   visible     Boolean
-  owner       User    @relation("ItemsOwned", fields: [ownerId], references: [id])
+  owner       User     @relation("ItemsOwned", fields: [ownerId], references: [id])
   ownerId     Int
-  borrower    User?   @relation("ItemsBorrowed", fields: [borrowerId], references: [id])
+  borrower    User?    @relation("ItemsBorrowed", fields: [borrowerId], references: [id])
   borrowerId  Int?
 }
```


